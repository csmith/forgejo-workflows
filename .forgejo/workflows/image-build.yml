name: Build and push container

permissions:
  contents: read
  packages: write

on:
  workflow_call:
    inputs:
      dockerfile:
        required: false
        type: string
        default: 'Dockerfile'
      image:
        required: false
        type: string
        default: ${{ github.repository }}
      github:
        required: false
        type: string
        default: ''

jobs:
  docker:
    runs-on: docker 
    steps:
      - name: Checkout
        uses: https://git.yak-wall.ts.net/public/actions/checkout@master

      - name: Get tags
        id: tags
        uses: https://git.yak-wall.ts.net/public/actions/imagetags@master

      - name: Login to forgejo
        uses: https://git.yak-wall.ts.net/public/actions/dockerlogin@master
        with:
          registry: git.yak-wall.ts.net
          username: chris
          password: ${{ secrets.PACKAGE_REGISTRY_TOKEN }}

      - name: Login to GitHub
        uses: https://git.yak-wall.ts.net/public/actions/dockerlogin@master
        if: ${{ inputs.github }}
        with:
          registry: ghcr.io
          username: csmith
          password: ${{ secrets.GH_REGISTRY_TOKEN }}

      - name: Build container
        uses: https://git.yak-wall.ts.net/public/actions/dockerbuild@master

      - name: Push to forgejo 
        uses: https://git.yak-wall.ts.net/public/actions/dockerpush@master
        with:
          name: git.yak-wall.ts.net/${{ inputs.image }}
          tags: ${{ steps.tags.outputs.tags }}

      - name: Push to GitHub
        uses: https://git.yak-wall.ts.net/public/actions/dockerpush@master
        if: ${{ inputs.github }}
        with:
          name: ghcr.io/${{ inputs.github }}
          tags: ${{ steps.tags.outputs.tags }}
